# Prompt and Spec Cleanup

Update writing.md to remove the dangling reference now that subdirectory assets are auto-assembled, and update the ClassRepository contract spec to document the new subdirectory assembly behavior.
