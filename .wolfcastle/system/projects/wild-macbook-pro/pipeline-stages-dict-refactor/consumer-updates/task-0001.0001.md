# Fix testConfig and daemon_test.go for dict-format stages

Update testConfig() in daemon_test.go to use map[string]config.PipelineStage and StageOrder. Also update all 5 stage config overrides within daemon_test.go itself.
