# Update pipeline prompt assembly

In internal/pipeline/prompt.go (line 15): the AssemblePrompt function currently receives a config.PipelineStage value and uses stage.PromptFile and stage.AllowedCommands. Since the Name field is removed from PipelineStage, if any logging or error messages in this function reference the stage name, add a stageName string parameter to the function signature. If the function does not reference the name, the signature can stay as-is (the PipelineStage struct still has all the fields it uses). Check all callers and update them accordingly. Update tests in prompt_test.go.
