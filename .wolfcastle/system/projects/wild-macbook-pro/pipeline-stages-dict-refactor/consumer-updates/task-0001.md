# Update daemon iteration to use StageOrder with map lookup

In internal/daemon/iteration.go (lines 60-68): replace 'for _, stage := range d.Config.Pipeline.Stages' with iteration over d.Config.Pipeline.StageOrder, looking up each stage by name from the Stages map. The intake-skip logic (line 66: stage.Name == 'intake') becomes a comparison against the loop variable (name == 'intake'). The model lookup (line 82: d.Config.Models[stage.Model]) stays the same since stage still has a Model field. The prompt assembly call (line 77) needs to pass the stage name as a separate argument if prompt.go's signature changes. Update all tests in iteration_test.go to construct configs with map-format stages and StageOrder.
