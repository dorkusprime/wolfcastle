# Update PipelineConfig and PipelineStage structs

In internal/config/types.go: (1) Change PipelineConfig.Stages from []PipelineStage to map[string]PipelineStage with json tag 'stages'. (2) Add StageOrder []string field with json tag 'stage_order'. (3) Remove the Name field from PipelineStage (the name is now the map key). (4) Keep all other PipelineStage fields (Model, PromptFile, Enabled, SkipPromptAssembly, AllowedCommands) and methods (IsEnabled, ShouldSkipPromptAssembly) unchanged. Reference the dict-format stages spec for the exact schema.
