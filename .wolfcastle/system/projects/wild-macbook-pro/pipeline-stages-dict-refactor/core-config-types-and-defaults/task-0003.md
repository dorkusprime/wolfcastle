# Update stage validation rules

In internal/config/validate.go: (1) Replace 'len(cfg.Pipeline.Stages) == 0' check with 'len(cfg.Pipeline.Stages) == 0' on the map (same semantic, different type). (2) Remove the duplicate-name check (lines 27-35) since map keys are inherently unique. (3) Remove the empty-name check since empty map keys are handled at parse time. (4) Add stage_order integrity validation: every entry in StageOrder must exist as a key in Stages, every key in Stages must appear in StageOrder, and StageOrder must not contain duplicates. (5) Keep model reference validation but iterate via map values. (6) Keep prompt_file non-empty validation but iterate via map values. (7) Update all tests in validate_test.go to construct configs with map-format stages and StageOrder. Maintain >90% coverage.
