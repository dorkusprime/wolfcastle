# Wire MigrateStagesFormat into ScaffoldService.Reinit

In the ScaffoldService.Reinit() method, add a call to migrator.MigrateStagesFormat() after the existing MigrateOldConfig() call. Follow the same error-discard pattern used by the other migration calls (log but do not propagate). Verify the call ordering is: MigrateDirectoryLayout, MigratePromptLayout (if it exists), MigrateOldConfig, MigrateStagesFormat.
