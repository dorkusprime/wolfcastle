# Revert partial JSONOutput→JSON migration

Audit gap-app-refactor-2: Only 3 files use app.JSON while 30+ still use app.JSONOutput. The cobra flag binding (root.go:56) targets JSONOutput. Having two fields is worse than one. Remove the JSON field from App struct, revert the 3 callers (orchestrator/criteria.go, task/unblock.go, project/create.go) back to app.JSONOutput, and remove the Init() line that copies JSONOutput→JSON.
