# Add Missing Command Sections

Write spec sections for all 15 commands that exist in the CLI but are missing from docs/specs/2026-03-12T00-06Z-cli-commands.md. The missing commands are: execute, intake, archive delete, archive restore, config show, config set, config append, config remove, config unset, knowledge add, knowledge edit, knowledge prune, knowledge show, completion, and help. Existing detailed specs should be referenced and condensed into the CLI commands spec format: Synopsis, Description, Arguments and Flags, Behavior, Output, Exit Codes, Examples, Error Cases.
