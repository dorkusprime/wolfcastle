# Add execute and intake command sections

Add spec sections for the two new foreground lifecycle commands. Both run the daemon loop in the foreground with interleaved output streaming. Use 'wolfcastle execute --help' and 'wolfcastle intake --help' for behavior details. Place these sections in the Lifecycle group after 'wolfcastle start' (execute) and in the appropriate position (intake). Follow the existing spec format: Synopsis, Description, Arguments and Flags, Behavior, Output, Exit Codes, Examples, Error Cases.
