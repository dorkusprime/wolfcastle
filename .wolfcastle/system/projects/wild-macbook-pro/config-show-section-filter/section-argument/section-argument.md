# Section Argument

Add optional positional argument to config show that filters output to a single top-level config key. When provided, the resolved config map is narrowed to just the named section before rendering. Works with all existing modes (default, --tier, --raw) and both output formats (human, --json).
