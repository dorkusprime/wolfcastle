# Add tests for section filtering

Add tests to cmd/config/show_test.go covering: (1) config show pipeline outputs only the pipeline section, (2) config show --tier local identity filters the tier output to just the identity key, (3) config show --raw models filters the raw-merged output, (4) config show bogus-section returns an error, (5) config show pipeline --json wraps the filtered section in the standard envelope with action=config_show, (6) config show (no argument) continues to work as before, outputting the full config. Verify that the output for a known section round-trips through JSON and contains the expected keys.
