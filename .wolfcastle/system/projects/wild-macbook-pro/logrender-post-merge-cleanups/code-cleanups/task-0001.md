# Replace indexOf with bytes.IndexByte

In internal/logrender/reader.go, the hand-rolled indexOf function (lines ~243-251) reimplements bytes.IndexByte. Replace the single call site (line ~219) with bytes.IndexByte(data[consumed:], '\n'), add 'bytes' to the import block, and delete the indexOf function. Update or remove any tests that exercise indexOf directly.
