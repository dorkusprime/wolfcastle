# Add RenderToFile convenience method

Add RenderToFile(tmplName string, data any, destPath string) error to PromptRepository. This method calls ResolveTemplate to resolve and execute the template, then writes the result atomically to destPath (using os.WriteFile with 0644 permissions, creating parent directories as needed). This consolidates the resolve-execute-write pattern into a single call that every call site will use.
