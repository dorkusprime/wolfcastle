# Migrate ADR and spec generation to templates

In cmd/adr_create.go: replace the strings.Builder block (lines 64-75) and os.WriteFile (line 88) with a single RenderToFile call using adr.md.tmpl and ADRData struct. Handle the three branches (stdin body, --body flag, empty template). In cmd/spec.go: replace the fmt.Sprintf branches (lines 81-86) and os.WriteFile (line 87) with RenderToFile using spec.md.tmpl and SpecData struct. The cmd files will need access to a PromptRepository; wire it through the cobra command context or construct one in the Run function.
