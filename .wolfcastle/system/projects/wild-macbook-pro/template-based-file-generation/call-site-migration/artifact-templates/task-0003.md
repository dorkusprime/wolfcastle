# Migrate audit-task and task markdown generation to templates

In internal/project/project_create.go: replace the embedded FS ReadFile + os.WriteFile in WriteAuditTaskMD (lines 40-46) with RenderToFile using audit-task.md.tmpl. The function currently bypasses tierfs entirely; after migration it should resolve through the three-tier system so users can override it. In cmd/task/add.go: replace the strings.Builder in writeTaskMD (lines 192-200) with RenderToFile using task.md.tmpl and TaskData struct. Both call sites currently discard errors (best-effort); preserve that behavior by ignoring the RenderToFile error.
