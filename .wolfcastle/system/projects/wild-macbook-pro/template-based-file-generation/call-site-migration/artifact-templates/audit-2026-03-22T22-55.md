# Audit Report: Artifact Templates

**Node:** `template-based-file-generation/call-site-migration/artifact-templates`

**Completed:** 2026-03-22T22:55:55Z

**Verdict:** PASSED

## Summary

Migrated all four artifact file generators (ADR, spec, task, audit-task) from inline string construction to three-tier template rendering via PromptRepository.RenderToFile. Created adr.md.tmpl, spec.md.tmpl, task.md.tmpl, and audit-task.md.tmpl under internal/project/templates/artifacts/. Fixed ResolveTemplate path resolution to match tier-FS layout. Added 10 golden-file snapshot tests and equivalence tests verifying byte-identical output. Fixed an import cycle in prompt_test.go. Audit found no correctness issues; all findings (duplicate struct definitions, unused struct fields, missing error-discard comment) are code hygiene items documented for future cleanup.

## Audit Trail

- `template-based-file-generation/call-site-migration/artifact-templates` [2026-03-22 22:24]: Created adr.md.tmpl, spec.md.tmpl, and task.md.tmpl under internal/project/templates/artifacts/. Each template uses typed struct fields (not map keys) and produces byte-identical output to the original inline string construction. Equivalence verified by 10 table-driven test cases covering body/no-body/multiline/whitespace-only inputs.
- `template-based-file-generation/call-site-migration/artifact-templates` [2026-03-22 22:34]: Migrated ADR and spec generation to templates. Replaced strings.Builder in adr_create.go and fmt.Sprintf in spec.go with RenderToFile('artifacts/adr.md', ADRData, path) and RenderToFile('artifacts/spec.md', SpecData, path). Fixed ResolveTemplate to resolve tier-FS paths directly (removed 'templates/' prefix) so artifact templates at artifacts/*.tmpl are reachable. Updated WithTemplate helper and repository_test.go tier-override test. Seeded artifact templates in cmd test helper via seedArtifactTemplates.
- `template-based-file-generation/call-site-migration/artifact-templates` [2026-03-22 22:42]: Migrated audit-task and task MD generation to templates. WriteAuditTaskMD now takes a *pipeline.PromptRepository and resolves artifacts/audit-task.md through the tier FS. writeTaskMD now takes a *pipeline.PromptRepository and renders artifacts/task.md with pipeline.TaskData. Created artifacts/audit-task.md.tmpl (static, no template vars). Updated callers in cmd/project/create.go and cmd/task/add.go. Updated tests in audit_coverage_test.go and task_coverage_test.go to construct PromptRepository with seeded templates.
- `template-based-file-generation/call-site-migration/artifact-templates` [2026-03-22 22:51]: Added 10 golden-file snapshot tests in internal/pipeline/template_test.go covering all 4 artifact templates: ADR (with/without body), spec (with body, without body, stdin multiline), task (with body, minimal, all optional fields, whitespace-only body), and audit-task (nil ctx raw). Fixed import cycle in prompt_test.go by replacing project.Templates embed.FS walk with disk-based filepath.WalkDir. All pipeline tests pass, 97.3% coverage.
- `template-based-file-generation/call-site-migration/artifact-templates` [2026-03-22 22:55]: Audit reviewed 18 files across templates, pipeline, cmd, and tests. PASS. Six findings: (1) gofmt alignment in repository_test.go — fixed in-audit. (2) Duplicate ADRData/SpecData structs in cmd/ and pipeline/ — structurally identical, templates work via reflection, acceptable as-is. (3) AuditTaskData struct and 8 TaskData fields unused by templates — forward-looking stubs from infra task, zero runtime cost, acceptable. (4) audit-task.md duplicated in audits/ and artifacts/ — flagged in task-0003 AAR, conscious scope decision, acceptable. (5) WriteAuditTaskMD discards error without comment — existing behavior was already best-effort, tests verify no-panic on failure paths, acceptable though a comment would help. (6) TestInitCmd_AlreadyInitialized flaky in full suite — pre-existing, not from this node. Build clean, go vet clean, all tests pass (flaky failure unrelated). Template output is byte-identical to original inline construction across all 10 snapshot cases. Quality: thorough, well-tested migration with good AAR documentation of tradeoffs.

