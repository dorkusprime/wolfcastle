# Snapshot tests for scaffold templates

Write snapshot tests that capture the output of each scaffold-generated file (gitignore and all five READMEs) and assert byte-for-byte identity with the expected content. Use Go test fixtures or golden files. These tests verify the migration introduced no regressions. Test both the Init and Reinit paths to confirm templates are correctly resolved from the tier system.
