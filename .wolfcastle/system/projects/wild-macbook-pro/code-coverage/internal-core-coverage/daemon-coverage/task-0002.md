# Cover Run and RunOnce main loop branches

Run is at 68.7% and RunOnce at 83.6%. These are the daemon's core execution loop and single-iteration entry point.

Run (daemon.go:207) uncovered branches:
- Stop-file detection mid-loop
- Error from RunOnce propagation
- IterationStop handling
- IterationError with non-context-cancelled error
- Clean shutdown via context cancellation vs signal
- workAvailable channel select with context done racing

RunOnce (daemon.go:354) uncovered branches:
- Navigate returning no-work when inbox has pending items
- Multiple tasks found but first one fails
- Index reload after iteration failure
- Stale scope node detection

Tests should use the existing test harness pattern (mock invoker, temp wolfcastle dir) seen in daemon_test.go and edge_cases_test.go.
