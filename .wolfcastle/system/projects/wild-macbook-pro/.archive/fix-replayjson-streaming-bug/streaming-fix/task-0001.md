# Rewrite replayJSON to stream and decompress

Replace os.ReadFile with os.Open + buffered scanning in replayJSON (cmd/daemon/follow.go). For paths ending in .gz, wrap the file handle in a gzip.NewReader before scanning, matching the pattern in ReplayReader.readFile (internal/logrender/reader.go:44-59). Each line should be written to stdout individually rather than the entire file contents at once. Ensure gzip readers and file handles are closed properly (defer).
