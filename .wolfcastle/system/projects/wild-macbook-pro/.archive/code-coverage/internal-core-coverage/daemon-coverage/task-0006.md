# Cover lock, PID, branch, and propagation gaps

Several support functions have moderate coverage gaps:

Lock (lock.go):
- AcquireGlobalLock (75.0%): lock file already exists with live process, lock file exists with dead process (stale lock cleanup), MkdirAll failure
- ReleaseGlobalLock (75.0%): lock file doesn't exist, remove failure
- ReadGlobalLock (80.0%): file not found, malformed JSON
- globalLockPath (88.9%): edge case in path construction

PID (pid.go):
- IsProcessRunning (80.0%): process exists but not owned, negative PID

Branch (branch.go):
- currentBranch (93.3%): detached HEAD state, git command failure

Propagation (propagate.go):
- propagateState (90.5%): parse error in node address, save failure after state change

checkGitProgress (deliverables.go, 95.2%): git command failure path
