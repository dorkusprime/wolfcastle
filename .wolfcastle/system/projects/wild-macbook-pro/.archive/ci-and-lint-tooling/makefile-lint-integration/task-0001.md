# Wire golangci-lint into Makefile lint target

The Makefile lint target (line 30) currently depends only on vet and fmt. Add a golangci-lint target that runs 'golangci-lint run ./...' and wire it as a dependency of lint alongside vet and fmt. The .golangci.yml config already exists at the repo root with v2 format, so no config work is needed. Also update the help target's lint description to mention golangci-lint.
