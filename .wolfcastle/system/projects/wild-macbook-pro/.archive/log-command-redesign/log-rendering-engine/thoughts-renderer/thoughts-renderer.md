# Thoughts Renderer

The --thoughts output mode. Extracts only assistant-type records and prints their raw text content with no decoration: no stage headers, no timestamps, no glyphs, no durations. Works identically in replay and follow mode (the only difference is whether records arrive all at once or incrementally). This is the simplest renderer.
