# Remove dead deprecated fields Store and Commit from App struct

Audit gap-app-refactor-4: App.Store (*state.StateStore) and App.Commit (string) have zero callers outside Init(). Store is aliased to State at Init line 104 but nothing reads Store. Commit is never read anywhere. Remove both fields and their Init() assignments. Verify build passes.
