# ContextBuilder

Build ContextBuilder in internal/pipeline: composes render methods from domain types with class and summary prompts from repositories. Step 10 of migration path. Depends on ClassRepository.
