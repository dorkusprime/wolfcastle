# Add JSON output for archive status

Ensure the JSON output mode includes archive information: an 'archived' count in the summary, and when --archived is used, the full archived node details with completion and archive timestamps. Update the output.Ok data map to include these fields.
