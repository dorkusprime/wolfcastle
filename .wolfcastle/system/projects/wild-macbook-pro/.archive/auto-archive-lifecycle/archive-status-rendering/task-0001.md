# Add archived node filtering to status tree rendering

Modify showTreeStatus() in cmd/daemon/status.go to filter out archived nodes from the default view. When archived nodes exist, append a summary line after the tree: 'N archived nodes (use --archived to view)'. The filtering should work with the archive state model defined in the spec (reading archived flag/status from RootIndex entries or checking the archive storage).
