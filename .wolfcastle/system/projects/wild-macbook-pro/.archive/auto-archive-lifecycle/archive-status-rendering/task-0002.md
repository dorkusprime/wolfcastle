# Implement --archived flag for status command

Add a --archived boolean flag to the status command that, when set, shows ONLY archived nodes. The display should include archive-specific metadata: when the node was completed, when it was archived, and its original address. Wire the flag through newStatusCmd(), showTreeStatus(), and the JSON output path.
