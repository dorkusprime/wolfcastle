# Update archive add CLI to perform full move

Modify cmd/archive_add.go to call the shared archive move function instead of only generating the Markdown rollup. After this change, wolfcastle archive add --node <addr> will: (1) validate preconditions (node complete, not already archived, is root-level), (2) generate Markdown rollup, (3) move state directories to .archive/, (4) update RootIndex (move to ArchivedRoot, flag entries as archived). The JSON output should include the same fields plus any new ones (e.g., archived_at). Update the command's help text to reflect the expanded behavior.
