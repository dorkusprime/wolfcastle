# Add archive restore CLI command

Create cmd/archive_restore.go with a cobra command registered under archiveCmd. Usage: wolfcastle archive restore --node <addr>. The command loads the RootIndex, validates the node is archived, calls the restore logic, and reports success. Support --json output following the existing pattern in archive_add.go (output.Ok with action/node fields). Register in init() under archiveCmd. Follow the existing CLI patterns: app.RequireIdentity(), flag parsing, error formatting.
