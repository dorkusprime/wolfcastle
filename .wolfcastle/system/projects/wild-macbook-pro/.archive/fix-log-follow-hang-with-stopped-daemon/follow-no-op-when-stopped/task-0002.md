# Add tests for explicit --follow with stopped daemon

Add test cases to cmd/daemon/follow_test.go (or follow_coverage_test.go, whichever is the appropriate home) that verify: (1) explicit --follow with a stopped daemon falls through to replay mode and exits, (2) explicit --follow with a running daemon still enters follow mode, (3) no --follow with a stopped daemon uses replay mode (existing behavior, regression guard). The test should use a mock or fake DaemonRepository with a controllable IsAlive() return value. Check existing test patterns in the file for how daemon state is mocked.
