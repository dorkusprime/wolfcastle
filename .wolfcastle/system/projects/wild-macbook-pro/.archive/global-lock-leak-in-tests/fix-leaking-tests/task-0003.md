# Redirect GlobalLockDir in start_deep_coverage_test.go

TestStartCmd_StalePIDRecoveredBeforeStart (line ~53) in cmd/daemon/start_deep_coverage_test.go invokes the start command without redirecting GlobalLockDir.

Add the standard redirection pattern:

    lockDir := t.TempDir()
    dmn.GlobalLockDir = lockDir
    defer func() { dmn.GlobalLockDir = "" }()

Ensure the dmn import (internal/daemon) is present.
