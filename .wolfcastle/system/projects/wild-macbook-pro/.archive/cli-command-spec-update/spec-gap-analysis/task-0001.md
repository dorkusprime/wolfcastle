# CLI Spec Gap Manifest

Audit of `cmd/` source against `docs/specs/2026-03-12T00-06Z-cli-commands.md`.

Source: 43 commands. Spec header claims 39. Spec body documents 40 (header count is stale).

---

## 1. Commands Missing from Spec

- [ ] **`wolfcastle audit enrich`** — Appends enrichment text to a node's audit enrichment list. Duplicates silently ignored.
  - Source: `cmd/audit/enrich.go` → `newEnrichCmd`
  - Use: `audit enrich [text]`
  - Flags:
    - `--node` (string, required) — Target node address
  - Args: exactly 1 positional (enrichment text)
  - JSON action: `audit_enrich`

- [ ] **`wolfcastle orchestrator criteria`** — Appends a success criterion to an orchestrator node, or lists existing criteria with `--list`.
  - Source: `cmd/orchestrator/criteria.go` → `newCriteriaCmd`
  - Use: `criteria [criterion]`
  - Flags:
    - `--node` (string, required) — Target node address
    - `--list` (bool, default: false) — List current success criteria instead of adding
  - Args: 0–1 positional (criterion text; required unless `--list`)
  - JSON actions: `success_criteria` (list mode), `success_criteria_add` (add mode)
  - Note: introduces a new command group `orchestrator` not currently in the spec's group list

- [ ] **`wolfcastle task amend`** — Modifies fields on an unstarted task. Tasks in `in_progress` or `complete` state cannot be amended.
  - Source: `cmd/task/amend.go` → `newAmendCmd`
  - Use: `task amend`
  - Flags:
    - `--node` (string, required) — Task address: `node-path/task-id`
    - `--body` (string) — Replace task body/description
    - `--add-deliverable` ([]string) — Append deliverable (repeatable)
    - `--add-constraint` ([]string) — Append constraint (repeatable)
    - `--add-acceptance` ([]string) — Append acceptance criterion (repeatable)
    - `--add-reference` ([]string) — Append reference (repeatable)
    - `--type` (string) — Task type: discovery, spec, adr, implementation, integration, cleanup
    - `--integration` (string) — How this task connects to other work
  - JSON action: `task_amend`

---

## 2. Flags Missing from Existing Spec Entries

### `wolfcastle task add`
Spec documents only `--node` and a positional description. Source (`cmd/task/add.go`) registers 10 additional flags:

- [ ] `--body` (string) — Detailed task description/body
- [ ] `--stdin` (bool, default: false) — Read task body from stdin
- [ ] `--deliverable` ([]string) — Expected output file (repeatable)
- [ ] `--type` (string) — Task type: discovery, spec, adr, implementation, integration, cleanup
- [ ] `--class` (string) — Task class override (e.g., lang-go)
- [ ] `--constraint` ([]string) — Constraint: what not to do (repeatable)
- [ ] `--acceptance` ([]string) — Acceptance criterion (repeatable)
- [ ] `--reference` ([]string) — Reference material path (repeatable)
- [ ] `--integration` (string) — How this task connects to other work
- [ ] `--parent` (string) — Parent task ID for hierarchical decomposition

### `wolfcastle project create`
Spec documents only `--node` and a positional name. Source (`cmd/project/create.go`) registers 3 additional flags:

- [ ] `--type` (string, default: "leaf") — Node type: "leaf" or "orchestrator"
- [ ] `--description` (string) — Node description
- [ ] `--scope` (string) — Audit scope (for orchestrators)

Note: spec description says "The new node is an orchestrator" as if it's always orchestrator. Source defaults to `leaf`. The spec behavior text needs updating to reflect the `--type` flag.

### `wolfcastle spec create`
Spec documents `--node` and `--json`. Source (`cmd/spec.go`) registers 2 additional flags:

- [ ] `--body` (string) — Spec content
- [ ] `--stdin` (bool, default: false) — Read spec body from stdin

### `wolfcastle status`
Spec documents `--node`, `--all`, `--watch`, `--interval`, `--json`. Source (`cmd/daemon/register.go`) registers 1 additional flag:

- [ ] `--expand` (bool, default: false) — Show completed nodes expanded (default: collapsed)

---

## 3. Flag Value Discrepancies

### `wolfcastle status --interval`
- [ ] **Default value mismatch**: spec says `5`, source says `2` (`Float64P("interval", "n", 2, ...)`)
- [ ] **Missing shorthand**: spec omits `-n` shorthand for `--interval`

---

## 4. Spec Header Updates Required

- [ ] Command count in the spec header: change `39` to `43`
- [ ] Group list in the spec header: add `orchestrator` as a new subcommand group (currently lists: `audit`, `task`, `project`, `inbox`, `spec`, `adr`, `archive`, `install`)

---

## Summary

| Category | Count |
|----------|-------|
| Missing commands | 3 |
| Missing flags (task add) | 10 |
| Missing flags (project create) | 3 |
| Missing flags (spec create) | 2 |
| Missing flags (status) | 1 |
| Value discrepancies | 2 |
| Header updates | 2 |
| **Total gaps** | **23** |
