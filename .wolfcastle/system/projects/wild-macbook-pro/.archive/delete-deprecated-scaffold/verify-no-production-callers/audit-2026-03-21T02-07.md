# Audit Report: Verify No Production Callers

**Node:** `delete-deprecated-scaffold/verify-no-production-callers`

**Completed:** 2026-03-21T02:07:26Z

**Verdict:** PASSED

## Summary

Verified that no production code calls the deprecated Scaffold() or ReScaffold() functions. All ~60 call sites are in test files; scaffold_service.go is fully self-contained, and cmd/init.go and cmd/update.go use only ScaffoldService. Cataloged 9 test files: 6 for deletion (test only deprecated functions), 1 for editing (scaffold_readme_test.go has 1 deprecated test among 3 valid ones), 1 to keep (reinit_coverage_test.go tests the replacement), and 1 omitted from the original catalog (create_test.go calls detectIdentity() which lives in scaffold.go). Escalated that scaffold.go also contains non-deprecated production functions (WriteAuditTaskMD, CreateProject) that the deletion node must preserve.

## Findings

1 total (1 remediated, 0 open)

### Remediated

- **gap-verify-no-production-callers-1**: task-0002 catalog missed create_test.go:221 (TestDetectIdentity_ReturnsNonEmptyValues) which calls detectIdentity() from scaffold.go. When scaffold.go is deleted, this test breaks. The deletion node must account for 9 files, not 8. (fixed by delete-deprecated-scaffold/verify-no-production-callers)

## Audit Trail

- `delete-deprecated-scaffold/verify-no-production-callers` [2026-03-21 02:03]: Grepped entire codebase for Scaffold() and ReScaffold() calls. All invocations are in _test.go files (scaffold_test.go, scaffold_coverage_test.go, scaffold_chmod_test.go, scaffold_extra_test.go, deprecated_coverage_test.go, category_a_test.go). Zero production callers. scaffold_service.go has no references to either deprecated function -- it is fully self-contained. cmd/init.go and cmd/update.go use only ScaffoldService. Deletion is safe.
- `delete-deprecated-scaffold/verify-no-production-callers` [2026-03-21 02:04]: Catalog of test files calling deprecated Scaffold/ReScaffold:

FILES TO DELETE (only test deprecated functions):
1. internal/project/scaffold_test.go — 14 tests, all call deprecated Scaffold()/ReScaffold()/WriteBasePrompts()
2. internal/project/deprecated_coverage_test.go — 12 tests, all call deprecated Scaffold()/ReScaffold()/migrateOldConfig()
3. internal/project/category_a_test.go — 1 test (TestReScaffold_InvalidJSONInLocalConfig), calls deprecated Scaffold()/ReScaffold()
4. internal/project/scaffold_chmod_test.go — 8 tests, all call deprecated Scaffold()/ReScaffold()
5. internal/project/scaffold_coverage_test.go — 14 tests, all call deprecated Scaffold()/ReScaffold()/WriteBasePrompts()
6. internal/project/scaffold_extra_test.go — 10 tests, all call deprecated Scaffold()/ReScaffold()/WriteBasePrompts()/detectIdentity()

FILES TO EDIT (mix of deprecated and non-deprecated):
7. internal/project/scaffold_readme_test.go — 4 tests total. 3 use ScaffoldService.Init() (KEEP), 1 uses deprecated Scaffold() (REMOVE: TestScaffold_CreatesREADMEs)

FILES TO KEEP (no deprecated calls):
8. internal/project/reinit_coverage_test.go — 7 tests, ALL use ScaffoldService.Init()/Reinit(). Tests the replacement service error paths. Do NOT delete.

Total: 6 files to delete, 1 file to edit (remove 1 test function), 1 file to keep unchanged.
- `delete-deprecated-scaffold/verify-no-production-callers` [2026-03-21 02:07]: AUDIT REVIEW — Scope: Verified claims from task-0001 and task-0002 across internal/project/ (scaffold.go, scaffold_service.go, 8 test files, cmd/init.go, cmd/update.go, cmd/project/create.go, cmd/audit/approve.go). Verdict: PASS with one accepted finding and one escalation.

Findings:
(1) create_test.go:221 calls detectIdentity() from scaffold.go but was omitted from task-0002's catalog (listed 8 files, should be 9). Accepted: the core conclusion (zero production callers of Scaffold/ReScaffold) is unaffected, and the gap is now documented for the deletion node.
(2) Escalated: scaffold.go contains non-deprecated production functions WriteAuditTaskMD and CreateProject, called from cmd/project/create.go and cmd/audit/approve.go. The sibling remove-deprecated-files node must extract or preserve these before deleting deprecated code.

Quality: task-0001 was thorough and accurate — the zero-production-callers claim holds up. task-0002's catalog is solid and the delete/edit/keep classification is correct, with the minor omission of create_test.go noted above. The breadcrumb trail is detailed enough for the deletion node to act on without re-investigation.

