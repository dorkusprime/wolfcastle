# Verify and test unknown-field detection for dict-format stages

In internal/config/unknown.go: the diffKeys function currently has special-case handling for arrays (lines 57-69) that iterates slice elements by index. With dict-format stages, pipeline.stages becomes a nested map, so the existing recursive map traversal (lines 43-55) handles it naturally. Verify this by: (1) confirming that diffKeys recurses into pipeline.stages.<name> as a map, not as an array element, (2) adding test cases in unknown_test.go that prove typos inside stage configs (e.g. 'promt_file' inside pipeline.stages.execute) are detected with the correct dot-notation path (pipeline.stages.execute.promt_file), (3) verifying that valid stage keys are not flagged as unknown. The array special-case code in lines 57-69 may become dead code for stages but is still needed for other array fields (like allowed_commands), so leave it in place. Maintain >90% coverage.
