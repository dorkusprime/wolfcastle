# Rewrite Defaults() for dict-format stages

In internal/config/config.go Defaults() function (lines 34-56): rewrite the two default stages ('intake' and 'execute') from a []PipelineStage slice literal to a map[string]PipelineStage map literal, keyed by stage name. Add StageOrder: []string{'intake', 'execute'} to the PipelineConfig. Remove the Name field from each stage value since the name is now the map key. Preserve all other field values (Model, PromptFile, AllowedCommands) exactly as they are.
