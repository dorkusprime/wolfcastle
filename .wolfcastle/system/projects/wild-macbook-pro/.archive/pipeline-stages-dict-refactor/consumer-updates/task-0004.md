# Ensure >90% test coverage across all modified packages

Run go test -cover on internal/config, internal/daemon, internal/pipeline, and internal/project packages. Verify each maintains >90% line coverage. If any package is below threshold, add targeted test cases for uncovered paths. Pay particular attention to: (1) iteration with disabled stages in StageOrder, (2) StageOrder entries missing from the map (should be caught by validation, but test the error path), (3) empty StageOrder, (4) prompt assembly with various stage configurations. Report coverage percentages in the audit breadcrumb.
