# Config Write Spec

Write the specification for config set, unset, append, and remove commands. Defines CLI interface (flags, arguments, output), dot-notation path semantics, tier targeting, validation behavior, and error conditions. Must be completed before implementation begins.
