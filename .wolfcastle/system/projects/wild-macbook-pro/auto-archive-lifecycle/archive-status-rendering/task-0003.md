# Update --all flag to include archived nodes

Update the --all flag behavior: currently it shows all engineer namespaces. With archive support, --all should ALSO include archived nodes in the output (in addition to its cross-namespace behavior). Archived nodes should be visually distinguished in the tree (e.g., dimmed text, [archived] suffix, or a separate section).
