# Write tests for shared archive service and updated archive add

Test the extracted shared move function: successful archive (verify directories moved, index updated), precondition failures (not complete, already archived, not root-level). Test the updated archive add CLI command end-to-end: verify it performs the full move, not just markdown generation. Verify the daemon still works correctly after refactoring to use the shared function. Use existing test patterns.
