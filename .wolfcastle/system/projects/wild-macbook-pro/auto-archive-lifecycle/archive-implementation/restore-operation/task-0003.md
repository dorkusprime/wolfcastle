# Write restore tests with >90% coverage

Add tests for restoreNode covering: successful round-trip (archive then restore, verify directories and index state), precondition failures (node not found, not archived, not in ArchivedRoot), partial restore recovery (archive dir exists but index not yet updated). Use the existing test patterns: testutil.Environment builder, mock clock, setupCompletedOrchestrator helpers. Verify the directory structure is symmetric with archive. Test the CLI command registration and flag validation.
