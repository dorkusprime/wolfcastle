# Add auto-archive configuration wiring

Wire the ArchiveConfig into the Daemon struct so the auto-archive timer can read it. Ensure the config is available at daemon construction time (in New() or via the Config field). Add an archive_poll_interval_seconds field to DaemonConfig if using a separate timer, or document why the existing poll interval suffices.
