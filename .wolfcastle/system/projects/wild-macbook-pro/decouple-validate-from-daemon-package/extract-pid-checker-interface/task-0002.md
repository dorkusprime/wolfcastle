# Add compile-time interface satisfaction check

Add a compile-time assertion in the daemon package (in repository.go or a dedicated file) that confirms *DaemonRepository satisfies validate.PIDChecker. Use the standard Go pattern: var _ validate.PIDChecker = (*DaemonRepository)(nil). This ensures future changes to either the interface or the struct produce a compile error rather than a silent runtime failure. Note: this import direction (daemon importing validate) is acceptable because validate is the interface owner and daemon is the implementor. If this creates an import cycle, place the assertion in an internal test file (repository_test.go) or a dedicated integration test instead.
