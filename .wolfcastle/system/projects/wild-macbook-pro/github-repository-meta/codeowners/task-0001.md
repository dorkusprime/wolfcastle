# Create CODEOWNERS file

Create .github/CODEOWNERS mapping the entire repository to @dorkusprime for automatic review assignment on PRs. The file should use a wildcard rule '* @dorkusprime' as the baseline, since the sole maintainer owns all packages. Place the file at .github/CODEOWNERS (GitHub's preferred location when a .github directory already exists).
