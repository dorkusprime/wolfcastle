# Audit Report: Config Validate Implementation

**Node:** `config-validate-command/config-validate-implementation`

**Completed:** 2026-03-23T00:00:50Z

**Verdict:** PASSED

## Summary

Implemented wolfcastle config validate as a new subcommand under config. The command loads and validates the resolved configuration, reporting structural issues by default and full cross-reference and identity checks with --full. Output supports both human-readable (severity/category/description per line with summary counts) and JSON (structured envelope with issues array) modes. Exit code 1 on errors, 0 on warnings-only or clean. Registered in cmd/config/register.go alongside show, set, unset, append, remove. Test suite covers 23 scenarios at 100% function coverage including edge cases (malformed JSON, missing tiers, unreadable files, mixed warnings and errors). CLI documentation maps every structural and full-mode check to the underlying validation functions.

## Audit Trail

- `config-validate-command/config-validate-implementation` [2026-03-22 23:53]: Implemented config validate command in cmd/config/validate.go. Registered in register.go. Supports --full flag and --json output. Parses ValidateStructure/Validate errors into structured issues. Build and tests pass.
- `config-validate-command/config-validate-implementation` [2026-03-22 23:57]: Added cmd/config/validate_test.go with 23 tests. 100% coverage on validate.go. All tests pass.
- `config-validate-command/config-validate-implementation` [2026-03-22 23:58]: Created docs/humans/cli/config-validate.md with full CLI documentation: command synopsis, flags (--full, --json), exit codes, output format for both human and JSON modes, structural vs full validation breakdown, CI/pre-commit examples, and See Also links.
- `config-validate-command/config-validate-implementation` [2026-03-23 00:00]: Audit reviewed 3 deliverables: cmd/config/validate.go (153 lines), cmd/config/validate_test.go (701 lines, 23 tests), docs/humans/cli/config-validate.md (115 lines). PASS. Build clean, all tests pass, 100% coverage on validate.go, race detector clean, vet clean. Two accepted findings: (1) JSON envelope uses output.Ok even for error cases, making ok=true when errors exist; error state conveyed via error_count and exit code. Tests explicitly verify this behavior. Documented quirk, not a defect. (2) splitValidationError string parsing is coupled to the format string in internal/config/validate.go; a future refactor to return structured errors would decouple them, but the current approach works correctly and is well-tested. No gaps recorded.

