# Add DurationMS field to Record struct

In internal/logrender/record.go, add a DurationMS *int64 field to the Record struct with json tag duration_ms. Use a pointer so zero-value (absent field) is distinguishable from an explicit 0. Place the field near the other timing-related fields (after ExitCode is a natural spot).
