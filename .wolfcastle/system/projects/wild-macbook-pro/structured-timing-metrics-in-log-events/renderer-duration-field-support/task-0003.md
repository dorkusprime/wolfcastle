# Add renderer tests for duration_ms consumption

Write or extend tests for the summary and interleaved renderers to verify: (1) when a stage_complete record contains duration_ms, the rendered duration matches the pre-computed value (not the timestamp diff), (2) when a stage_complete record lacks duration_ms, the rendered duration is computed from timestamps as before, (3) ParseRecord correctly round-trips a duration_ms field. Use the existing test patterns in internal/logrender/.
