# Task Classes

Dedicated task classes documentation page (docs/humans/task-classes.md). Covers what classes are (behavioral prompts, not capability gates), the --class flag with fallback resolution, full tables of built-in language/framework/non-language classes, custom class and framework creation, prompt override mechanics, and the universal prompt. Links to Configuration Guide for tier mechanics and Config Reference for task_classes config fields.
