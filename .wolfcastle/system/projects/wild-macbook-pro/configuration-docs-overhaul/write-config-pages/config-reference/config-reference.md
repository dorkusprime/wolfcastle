# Config Reference

Field-by-field reference page (docs/humans/config-reference.md). Every config field organized by section: identity, daemon, pipeline, models, retries, failure, git, overlap, archive, task_classes, validation_commands. Each field gets type, default value, description, and example. This is the lookup page for 'what does daemon.stall_timeout_seconds do?' Cross-references back to the Configuration Guide for context on how fields interact.
