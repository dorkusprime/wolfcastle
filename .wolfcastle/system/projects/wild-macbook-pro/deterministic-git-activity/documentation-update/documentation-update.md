# Documentation Update

Update all human-facing documentation to accurately describe the new deterministic git behavior where the daemon commits on both success and failure, controlled by git config fields. The agent no longer touches git.
