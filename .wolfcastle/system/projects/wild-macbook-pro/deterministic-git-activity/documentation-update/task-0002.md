# Update configuration.md with new git config fields

Update docs/humans/configuration.md to document the full git config section: auto_commit (master switch), commit_on_success, commit_on_failure, commit_state, skip_hooks. For each field, document: the default value, what it controls, and how it interacts with other fields (e.g., fine-grained fields are ignored when auto_commit is false). Include an example JSON snippet showing the full git config block.
