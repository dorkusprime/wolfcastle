# Git Config Expansion

Add the new git config fields (commit_on_success, commit_on_failure, commit_state) to the config struct, defaults, and validation. These fields control the daemon's commit behavior per the deterministic-git spec.
