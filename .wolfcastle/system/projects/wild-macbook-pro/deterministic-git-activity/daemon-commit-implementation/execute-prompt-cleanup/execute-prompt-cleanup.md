# Execute Prompt Cleanup

Remove all git instructions from the execute prompt template and related agent-facing docs. The agent should never run git add, git commit, or any git commands. The daemon handles everything.
