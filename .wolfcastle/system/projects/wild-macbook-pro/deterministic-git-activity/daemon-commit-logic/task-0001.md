# Refactor autoCommitPartialWork into a configurable daemon commit function

The current autoCommitPartialWork (iteration.go:549) is hardcoded for the failure path. Refactor it into a function that accepts a commit reason (success or failure), an attempt number (for failure messages), and the full GitConfig. The function should: (1) Check auto_commit is enabled. (2) Check the path-specific flag (commit_on_success or commit_on_failure). (3) Stage changes: git add -u for code changes; git add .wolfcastle/ only if commit_state is true. (4) Use the spec's commit message format: success = 'wolfcastle: <task-id> complete', failure = 'wolfcastle: <task-id> partial (attempt <N>)'. (5) Respect skip_hooks. The existing autoCommitPartialWork call site (iteration.go:384) should use the refactored function.
