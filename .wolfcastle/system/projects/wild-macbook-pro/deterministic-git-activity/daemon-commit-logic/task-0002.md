# Add success-path commit to iteration loop

In the success path of RunOnce (around iteration.go:358-373, after state propagation), call the refactored commit function with reason=success. This is the counterpart to the failure-path call at line 384. The commit should happen after all state mutations (task completion, parent propagation) are done, so the commit captures the final state. If commit_on_success is false, skip it. Wire the Daemon's Config.Git through to the call.
