# Remove Phase H and all git instructions from execute.md

In internal/project/templates/prompts/stages/execute.md: (1) Remove the entire Phase H (Commit) section (lines 151-158 currently). (2) Remove the rule 'Commit before signaling completion' (line 199). (3) Remove the line 'You must have committed changes before emitting this' from the WOLFCASTLE_COMPLETE description (line 168). (4) Remove the 'git add .wolfcastle/' instruction added in PR #137 if any remnants exist outside Phase H. (5) Re-letter subsequent phases (I->H, J->I, K->J). (6) Search for any other git references in the file and remove them. The terminal markers and signal behavior remain unchanged; only git operations are removed.
