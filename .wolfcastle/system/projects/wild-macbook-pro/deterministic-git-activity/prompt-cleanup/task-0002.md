# Update AGENTS.md and docs/agents/ to forbid git operations

Review AGENTS.md and all files in docs/agents/ for any guidance telling agents to commit, stage, or run git commands. Remove such guidance and replace with a clear statement: 'Agents must not run git add, git commit, or any git command. The daemon handles all git operations deterministically after each task iteration.' Also check the .wolfcastle/system/base/ copy of execute.md and ensure it matches the updated version.
