# Foreground Renderer Flags

Add output mode flags (--thoughts, --interleaved, --json) to the start command for foreground mode, defaulting to summary renderer instead of the hardcoded interleaved renderer. Wire the same renderer dispatch logic used by wolfcastle log.
