# Spec Update

Update the log-command-design spec's Non-Daemon Mode section to reflect that foreground mode defaults to summary (not interleaved) and accepts the same output mode flags as wolfcastle log.
