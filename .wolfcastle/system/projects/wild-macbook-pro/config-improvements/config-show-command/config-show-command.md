# Config Show Command

Add 'wolfcastle config show' command that displays the fully resolved configuration after three-tier merge, so users can debug override behavior and verify which tier is providing each value.
