# Verify draft and superseded specs

Check the 3 draft specs (tui, worktree-by-default, task-classes) and 1 superseded spec (wolfcastle-fs). For drafts: determine if the feature has been implemented (making the spec implementable and needing update) or if the draft is still aspirational. For superseded: confirm the superseding spec (domain-repository-architecture) is accurate and the superseded spec links to it correctly. Update the specs README index to reflect current status of all specs.
