# Verify CLI per-command docs

Check all 40+ docs/humans/cli/ per-command pages. For each: verify the command still exists, flags are current, examples work, and output format matches. Priority areas: (1) new command pages needed for config set, config unset, config append, config remove if not already present, (2) config-show.md must reflect --tier and --raw flags, (3) status.md must document --detail flag, (4) audit-aar.md and audit-report.md must exist and be accurate, (5) archive-add.md must reflect current behavior. Cross-reference against cmd/ directory to ensure no command lacks documentation.
