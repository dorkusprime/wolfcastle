# Verify README feature list and overview

Read README.md and verify the feature list and architecture summary are complete. Must mention: (1) stages-as-dict configuration, (2) config show/set/unset/append/remove commands, (3) audit aar and audit report commands, (4) auto-archive lifecycle, (5) unknown-field detection, (6) stall detector, (7) status --detail flag, (8) spec review pipeline stage, (9) CHILDREF_STATE_MISMATCH doctor check. The architecture summary should reflect the current module structure. Update where incomplete.
