# Verify CONTRIBUTING.md package map

Read CONTRIBUTING.md and compare its package map against actual internal/ packages. Current packages include ~20 directories: archive, clock, config, daemon, errors, git, invoke, logging, output, pipeline, project, selfupdate, signals, state, testutil, tierfs, tree, validate. The documented list shows 15 packages. Identify missing packages (likely git, tierfs, signals, and possibly others added recently). Verify existing package descriptions are still accurate. Update the table.
