#!/bin/sh
set -e

echo "pre-commit: checking formatting..."
bad=$(gofmt -l . 2>/dev/null | grep -v vendor/ || true)
if [ -n "$bad" ]; then
    echo "gofmt needed on:"
    echo "$bad"
    exit 1
fi

echo "pre-commit: ok"
